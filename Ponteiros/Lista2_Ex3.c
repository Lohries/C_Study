#include <stdio.h>
int main() {
    int  mat1[100], mat2[100], contador = 0, *prt;

    prt = mat;

    for (i = 0; x > i; i++){
        printf("Qual elemento sera inserido: ");
        scanf("%i", prt);
        printf("elemento %i: %i memeoria: %i ", contador, *prt, prt);
        prt++;
        contador++;
        printf("Voce quer continuar: ");
        scanf("%s", &confirm);
    }
    while(confirm == 'S');
    return 0;

}
